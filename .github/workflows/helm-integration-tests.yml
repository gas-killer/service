name: Helm Integration Test

on:
  push:
    branches:
      - main
      - dev
      - staging
  pull_request:
    branches:
      - main
      - dev
      - staging
  workflow_dispatch:

jobs:
  helm-test:
    name: Test with Helm Charts
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      # Run both schemes to completion even if one fails.
      fail-fast: false
      matrix:
        signature_scheme: [bls, schnorr]
    env:
      CI_ADMIN_KEY: ci-admin-key
      SIGNATURE_SCHEME: ${{ matrix.signature_scheme }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create kind cluster
        uses: helm/kind-action@v1.10.0
        with:
          cluster_name: gas-killer-test
          wait: 120s
          node_image: kindest/node:v1.27.3

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: "latest"

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Cache Rust dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      # Build test scripts early to avoid delays after router starts
      # The router has a 180s internal timeout, so we need to minimize time between
      # router startup and triggering tasks
      - name: Build test scripts
        run: |
          cargo build --release -p scripts --bin setup_schnorr_operators
          cargo build --release -p scripts --bin deploy_example
          cargo build --release -p scripts --bin send_request

      - name: Build Docker images
        run: |
          echo "Building router image..."
          docker build -f ./router/Dockerfile -t service:router-local .

          echo "Building node image..."
          docker build -f ./node/Dockerfile -t service:node-local .

      - name: Load images into kind cluster
        run: |
          echo "Loading router image into kind..."
          kind load docker-image service:router-local --name gas-killer-test

          echo "Loading node image into kind..."
          kind load docker-image service:node-local --name gas-killer-test

      - name: Pre-pull external images into kind
        run: |
          IMAGES=(
            "ghcr.io/breadchaincoop/ethereum:latest"
            "ghcr.io/breadchaincoop/eigenlayer:latest"
            "ghcr.io/breadchaincoop/target-contracts/bridge:latest"
          )
          for IMAGE in "${IMAGES[@]}"; do
            echo "Pulling $IMAGE..."
            docker pull "$IMAGE"
            echo "Loading $IMAGE into kind..."
            kind load docker-image "$IMAGE" --name gas-killer-test
          done

      - name: Verify cluster is ready
        run: |
          kubectl cluster-info
          kubectl get nodes
          kubectl get pods -A

      - name: Fetch Helm dependencies
        run: helm dependency update ./helm/gas-killer

      - name: Install Helm chart
        run: |
          # Set L1 fork URL from secret or use public Sepolia
          if [ -n "${{ secrets.HTTP_RPC }}" ]; then
            FORK_URL="${{ secrets.HTTP_RPC }}"
          else
            FORK_URL="https://ethereum-sepolia-rpc.publicnode.com"
          fi

          # Use Anvil default keys for testing
          PRIVATE_KEY="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
          FUNDED_KEY="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"

          # Sepolia EigenLayer contract addresses (matches the Sepolia fork URL above)
          DELEGATION_MANAGER="0xD4A7E1Bd8015057293f0D0A557088c286942e84b"
          STRATEGY_MANAGER="0x2E3D6c0744b10eb0A4e6F679F71554a39Ec47a5D"
          LST_CONTRACT="0x00c71b0fcadE911B2feeE9912De4Fe19eB04ca56"
          LST_STRATEGY="0x8b29d91e67b013e855EaFe0ad704aC4Ab086a574"
          ALLOCATION_MANAGER="0x42583067658071247ec8CE0A516A58f682002d07"

          echo "Installing Helm chart with fork URL: $FORK_URL"

          # Set L2 fork URL for local L2 Anvil (LOCAL mode)
          if [ -n "${{ secrets.L2_HTTP_RPC }}" ]; then
            L2_FORK_URL="${{ secrets.L2_HTTP_RPC }}"
          else
            L2_FORK_URL="https://rpc.gnosischain.com"
          fi

          # The l2 / bridge / yield-distribution subsystem exercises BLS-interface
          # target contracts (the bridge deploys a BLSSignatureChecker and a BLS
          # yield distributor). The schnorr router can only drive
          # SchnorrGasKillerSDK targets, so disable that subsystem for the schnorr
          # leg; the l1 SchnorrArraySummation flow below is independent of it.
          if [ "$SIGNATURE_SCHEME" = "schnorr" ]; then
            L2_FEATURES=false
          else
            L2_FEATURES=true
          fi

          # Install without --wait to avoid circular dependency
          # (pods wait for setup job, setup job is post-install hook)
          helm install gas-killer ./helm/gas-killer \
            --set global.environment=LOCAL \
            --set global.nodeCount=3 \
            --set eigenlayer.contracts.delegationManager="$DELEGATION_MANAGER" \
            --set eigenlayer.contracts.strategyManager="$STRATEGY_MANAGER" \
            --set eigenlayer.contracts.lstContract="$LST_CONTRACT" \
            --set eigenlayer.contracts.lstStrategy="$LST_STRATEGY" \
            --set eigenlayer.contracts.allocationManager="$ALLOCATION_MANAGER" \
            --set secrets.forkUrl="$FORK_URL" \
            --set secrets.privateKey="$PRIVATE_KEY" \
            --set secrets.fundedKey="$FUNDED_KEY" \
            --set secrets.l2ForkUrl="$L2_FORK_URL" \
            --set secrets.adminKey="$CI_ADMIN_KEY" \
            --set node.image.repository=service \
            --set node.image.tag=node-local \
            --set node.image.pullPolicy=Never \
            --set router.image.repository=service \
            --set router.image.tag=router-local \
            --set router.image.pullPolicy=Never \
            --set sharedData.storageClass="" \
            --set global.signatureScheme="$SIGNATURE_SCHEME" \
            --set l2.enabled=$L2_FEATURES \
            --set bridge.enabled=$L2_FEATURES \
            --set bridge.waitForBridge=$L2_FEATURES \
            --set yieldDistribution.enabled=$L2_FEATURES

      - name: Check deployment status
        run: |
          echo "=== Helm Release Status ==="
          helm status gas-killer

          echo "=== All Pods ==="
          kubectl get pods -o wide

          echo "=== All Services ==="
          kubectl get services

      - name: Wait for setup job to complete
        run: |
          echo "Waiting for setup job to complete..."

          # Find the actual setup job name (includes chart name)
          SETUP_JOB=$(kubectl get jobs -o name | grep setup | head -1)

          if [ -z "$SETUP_JOB" ]; then
            echo "Setup job not found!"
            kubectl get jobs
            exit 1
          fi

          echo "Found setup job: $SETUP_JOB"
          kubectl wait --for=condition=complete "$SETUP_JOB" --timeout=900s

          echo "Setup job completed successfully"
          kubectl logs "$SETUP_JOB" --tail=30

      - name: Wait for bridge job to complete
        run: |
          echo "Waiting for bridge job to complete..."

          # Find the bridge job
          BRIDGE_JOB=$(kubectl get jobs -o name | grep bridge | head -1)

          if [ -n "$BRIDGE_JOB" ]; then
            echo "Found bridge job: $BRIDGE_JOB"
            kubectl wait --for=condition=complete "$BRIDGE_JOB" --timeout=900s || {
              echo "Bridge job failed or timed out"
              kubectl logs "$BRIDGE_JOB" --tail=100 || true
              exit 1
            }
            echo "Bridge job completed successfully"
            kubectl logs "$BRIDGE_JOB" --tail=30
          else
            echo "No bridge job found (bridge may be disabled)"
          fi

      - name: Wait for yield distribution job to complete
        run: |
          echo "Waiting for yield distribution job to complete..."

          # Find the yield distribution job
          YIELD_JOB=$(kubectl get jobs -o name | grep yield-distribution | head -1)

          if [ -n "$YIELD_JOB" ]; then
            echo "Found yield distribution job: $YIELD_JOB"

            # Wait for the job to complete (success or failure)
            TIMEOUT=900
            ELAPSED=0
            while [ $ELAPSED -lt $TIMEOUT ]; do
              # Check job status
              STATUS=$(kubectl get "$YIELD_JOB" -o jsonpath='{.status.conditions[?(@.type=="Complete")].status}' 2>/dev/null || echo "")
              FAILED=$(kubectl get "$YIELD_JOB" -o jsonpath='{.status.conditions[?(@.type=="Failed")].status}' 2>/dev/null || echo "")

              if [ "$STATUS" = "True" ]; then
                echo "Yield distribution job completed successfully"
                # Get logs from the pod
                YIELD_POD=$(kubectl get pods -l job-name=$(echo "$YIELD_JOB" | sed 's|job/||') -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
                if [ -n "$YIELD_POD" ]; then
                  echo "=== Yield Distribution Pod Logs ==="
                  kubectl logs "$YIELD_POD" -c yield-distribution-test --tail=100 || true
                fi
                exit 0
              fi

              if [ "$FAILED" = "True" ]; then
                echo "Yield distribution job FAILED"
                # Get logs from the pod BEFORE it gets deleted
                YIELD_POD=$(kubectl get pods -l job-name=$(echo "$YIELD_JOB" | sed 's|job/||') -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
                if [ -n "$YIELD_POD" ]; then
                  echo "=== Yield Distribution Pod Logs (FAILED) ==="
                  echo "--- Init container: wait-for-bridge ---"
                  kubectl logs "$YIELD_POD" -c wait-for-bridge --tail=50 || true
                  echo "--- Init container: extract-addresses ---"
                  kubectl logs "$YIELD_POD" -c extract-addresses --tail=50 || true
                  echo "--- Main container: yield-distribution-test ---"
                  kubectl logs "$YIELD_POD" -c yield-distribution-test --tail=200 || true
                else
                  echo "Could not find yield distribution pod for logs"
                  kubectl get pods -o wide
                fi
                exit 1
              fi

              echo "Job still running... ($ELAPSED/$TIMEOUT seconds)"
              sleep 10
              ELAPSED=$((ELAPSED + 10))
            done

            echo "Timeout waiting for yield distribution job"
            # Try to get logs even on timeout
            YIELD_POD=$(kubectl get pods -l job-name=$(echo "$YIELD_JOB" | sed 's|job/||') -o jsonpath='{.items[0].metadata.name}' 2>/dev/null || echo "")
            if [ -n "$YIELD_POD" ]; then
              kubectl logs "$YIELD_POD" -c yield-distribution-test --tail=100 || true
            fi
            exit 1
          else
            echo "No yield distribution job found (yieldDistribution may be disabled)"
          fi

      - name: Wait for all pods to be ready
        run: |
          echo "Waiting for L2 pod..."
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/component=l2 --timeout=180s || echo "No L2 pod found"

          echo "Waiting for L1 pod..."
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/component=l1 --timeout=180s

          echo "Waiting for node pods..."
          # Show pod status before waiting
          echo "=== Current pod status ==="
          kubectl get pods -o wide

          # Try waiting, but if it fails, show debug info
          if ! kubectl wait --for=condition=ready pod -l app.kubernetes.io/component=node --timeout=300s; then
            echo "=== Node pods failed to become ready ==="
            kubectl get pods -o wide
            echo "=== Node-1 describe ==="
            kubectl describe pod -l gas-killer/node-index=1 || true
            echo "=== Node-1 logs ==="
            NODE1_POD=$(kubectl get pods -l gas-killer/node-index=1 -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
            if [ -n "$NODE1_POD" ]; then
              echo "Init container wait-for-setup:"
              kubectl logs $NODE1_POD -c wait-for-setup || true
              echo "Init container wait-for-l1:"
              kubectl logs $NODE1_POD -c wait-for-l1 || true
              echo "Main container node:"
              kubectl logs $NODE1_POD -c node || true
            fi
            exit 1
          fi

          echo "Waiting for router pod..."
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/component=router --timeout=300s

          echo "All pods are ready!"

      - name: Verify router persistence
        run: |
          # The router opens its SQLite store under DATA_DIR (/data) at startup and aborts if
          # the volume is missing or not writable by the non-root user. Assert the database
          # file exists so a persistence regression surfaces here instead of as a generic
          # readiness timeout or a downstream task failure.
          ROUTER_POD=$(kubectl get pods -l app.kubernetes.io/component=router -o jsonpath='{.items[0].metadata.name}')
          echo "Router pod: $ROUTER_POD"
          echo "=== Contents of /data ==="
          kubectl exec "$ROUTER_POD" -- ls -l /data
          echo "=== Asserting /data/router.db exists ==="
          kubectl exec "$ROUTER_POD" -- test -f /data/router.db
          echo "router.db is present and the persistent volume is writable"

      - name: Setup port forwarding for testing
        run: |
          # Find actual service names (they include the chart name)
          L1_SERVICE=$(kubectl get services -o name | grep '\-l1$' | head -1 | sed 's|service/||')
          ROUTER_SERVICE=$(kubectl get services -o name | grep router | head -1 | sed 's|service/||')

          echo "L1 service: $L1_SERVICE"
          echo "Router service: $ROUTER_SERVICE"

          # Port forward L1 RPC
          kubectl port-forward service/$L1_SERVICE 8545:8545 &

          # Port forward router ingress
          kubectl port-forward service/$ROUTER_SERVICE 8080:8080 &

          # Port forward L2 RPC (for yield distribution verification)
          L2_SERVICE=$(kubectl get services -o name | grep '\-l2$' | head -1 | sed 's|service/||')
          if [ -n "$L2_SERVICE" ]; then
            echo "L2 service: $L2_SERVICE"
            kubectl port-forward service/$L2_SERVICE 8546:8546 &
          fi

          # Wait for port forwards to be ready
          sleep 10

          echo "Port forwarding established"

      - name: Verify API key admin flow
        run: |
          # Exercises the full API-key lifecycle against the deployed router and its persistent
          # SQLite store: admin auth is enforced, a key is minted and listed (never leaking the
          # secret), and revocation is enforced immediately on the task path. Runs before the
          # on-chain trigger and does not disturb it — the revoked/invalid keys never enqueue a
          # task (auth is rejected before validation).
          set -euo pipefail
          ROUTER_URL=http://localhost:8080

          # A well-formed task body so /trigger reaches the handler's auth check rather than
          # failing JSON extraction first; field values are irrelevant because auth is evaluated
          # before validation.
          TASK_BODY='{"body":{"target_address":"0x0000000000000000000000000000000000000001","call_data":[171,205,239,1],"transition_index":0,"from_address":"0x0000000000000000000000000000000000000002","value":"0","block_height":1}}'

          echo "=== Admin endpoints reject requests without a credential ==="
          CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST "$ROUTER_URL/admin/keys")
          [ "$CODE" = "401" ] || { echo "expected 401 without admin credential, got $CODE"; exit 1; }

          echo "=== Mint an API key ==="
          CREATE=$(curl -s -X POST -H "Authorization: Bearer $CI_ADMIN_KEY" \
            -H "Content-Type: application/json" -d '{"label":"ci-e2e"}' "$ROUTER_URL/admin/keys")
          echo "create response: $CREATE"
          KEY=$(echo "$CREATE" | jq -r '.key')
          ID=$(echo "$CREATE" | jq -r '.id')
          case "$KEY" in gk_*) ;; *) echo "minted key missing gk_ prefix: $KEY"; exit 1;; esac

          echo "=== Listing shows the key id but never the secret ==="
          LIST=$(curl -s -H "Authorization: Bearer $CI_ADMIN_KEY" "$ROUTER_URL/admin/keys")
          echo "list response: $LIST"
          echo "$LIST" | jq -e --arg id "$ID" 'any(.[]; .id == $id)' >/dev/null \
            || { echo "created key not listed"; exit 1; }
          if echo "$LIST" | jq -e 'any(.[]; has("key"))' >/dev/null; then
            echo "listing leaked the key value"; exit 1
          fi

          echo "=== The minted key passes the /trigger auth gate ==="
          CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST -H "Authorization: Bearer $KEY" \
            -H "Content-Type: application/json" -d "$TASK_BODY" "$ROUTER_URL/trigger")
          [ "$CODE" != "401" ] || { echo "a valid key was rejected on /trigger"; exit 1; }

          echo "=== Revoke the key ==="
          CODE=$(curl -s -o /dev/null -w "%{http_code}" -X DELETE \
            -H "Authorization: Bearer $CI_ADMIN_KEY" "$ROUTER_URL/admin/keys/$ID")
          [ "$CODE" = "204" ] || { echo "expected 204 on revoke, got $CODE"; exit 1; }

          echo "=== The revoked key is rejected on /trigger ==="
          CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST -H "Authorization: Bearer $KEY" \
            -H "Content-Type: application/json" -d "$TASK_BODY" "$ROUTER_URL/trigger")
          [ "$CODE" = "401" ] || { echo "revoked key still accepted on /trigger (got $CODE)"; exit 1; }

          echo "API key admin flow verified"

          # Mint a working key for the remaining e2e steps, which authenticate /trigger with it.
          # The value is written to GITHUB_ENV (not echoed) so later steps pick it up.
          WORKING=$(curl -s -X POST -H "Authorization: Bearer $CI_ADMIN_KEY" \
            -H "Content-Type: application/json" -d '{"label":"ci-e2e-runner"}' "$ROUTER_URL/admin/keys")
          WORKING_KEY=$(echo "$WORKING" | jq -r '.key')
          case "$WORKING_KEY" in gk_*) ;; *) echo "failed to mint working key"; exit 1;; esac
          echo "GAS_KILLER_API_KEY=$WORKING_KEY" >> "$GITHUB_ENV"
          echo "provisioned working API key for downstream steps"

      - name: Verify shared data was created
        run: |
          echo "Checking if setup job created the required files..."

          # Get the setup job pod name
          SETUP_POD=$(kubectl get pods -l app.kubernetes.io/component=setup -o jsonpath='{.items[0].metadata.name}')

          echo "Setup pod: $SETUP_POD"

          # Check PVC
          kubectl get pvc

          echo "Setup job logs:"
          kubectl logs $SETUP_POD || true

      - name: Get AVS deployment file
        run: |
          echo "Copying avs_deploy.json from router pod..."

          ROUTER_POD=$(kubectl get pods -l app.kubernetes.io/component=router -o jsonpath='{.items[0].metadata.name}')

          mkdir -p config/.nodes
          kubectl cp $ROUTER_POD:/app/.nodes/avs_deploy.json ./config/.nodes/avs_deploy.json

          if [ ! -f "./config/.nodes/avs_deploy.json" ]; then
            echo "AVS deployment file not found"
            exit 1
          fi

          echo "AVS deployment file contents:"
          cat ./config/.nodes/avs_deploy.json

          # In schnorr mode the deploy binary PoP-registers each operator with the
          # SchnorrStakeRegistry, which needs the operator key files from the PVC.
          if [ "$SIGNATURE_SCHEME" = "schnorr" ]; then
            echo "Copying operator key files for Schnorr PoP registration..."
            kubectl cp "$ROUTER_POD:/app/.nodes/operator_keys" ./config/.nodes/operator_keys
            ls ./config/.nodes/operator_keys/*.private.ecdsa.key.json >/dev/null 2>&1 || {
              echo "Operator key files not found on the shared PVC"; exit 1; }
          fi

      - name: Deploy ArraySummation contract
        run: |
          echo "Deploying ArraySummation contract..."

          export HTTP_RPC=http://localhost:8545
          export WS_RPC=ws://localhost:8545
          export AVS_DEPLOYMENT_PATH="./config/.nodes/avs_deploy.json"
          export PRIVATE_KEY="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"

          # The array sizing lives in scripts/examples/examples.toml now, and the target is a
          # plain CREATE deploy from a locally built artifact — no pre-deployed factory.
          ./scripts/examples/fetch_examples.sh

          # The two schemes need different targets: ArraySummation inherits GasKillerSDK and
          # answers the BLS interface id, SchnorrArraySummation inherits SchnorrGasKillerSDK and
          # verifies against the stake registry. Deploying the wrong one fails the router's
          # ERC-165 admission gate.
          if [ "$SIGNATURE_SCHEME" = "schnorr" ]; then
            MANIFEST_EXAMPLE="schnorrArraySummation"
          else
            MANIFEST_EXAMPLE="arraySummation"
          fi

          # No-op under bls; registers the operator set before any target deploys under schnorr.
          ./target/release/setup_schnorr_operators
          ./target/release/deploy_example --example "$MANIFEST_EXAMPLE"

          echo "ArraySummation deployment completed"

      - name: Trigger Gas Killer multiple times and verify execution
        run: |
          echo "Triggering Gas Killer tasks..."

          export HTTP_RPC=http://localhost:8545
          export WS_RPC=ws://localhost:8545
          export AVS_DEPLOYMENT_PATH="./config/.nodes/avs_deploy.json"
          export GAS_KILLER_TRIGGER_URL=http://localhost:8080/trigger
          export PRIVATE_KEY="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
          # send_request authenticates /trigger with GAS_KILLER_API_KEY, minted in the admin-flow
          # step and exported via GITHUB_ENV.

          # Get ArraySummation address from deployment file
          ARRAY_SUMMATION_ADDRESS=$(jq -r '.addresses.arraySummation' "$AVS_DEPLOYMENT_PATH")
          echo "ArraySummation address: $ARRAY_SUMMATION_ADDRESS"

          # Get initial stateTransitionCount
          INITIAL_COUNT=$(cast call "$ARRAY_SUMMATION_ADDRESS" "stateTransitionCount()(uint256)" --rpc-url http://localhost:8545 | tr -d '[:space:]')
          echo "Initial stateTransitionCount: $INITIAL_COUNT"

          # Block height before triggering, used to bound the SumCalculated event query below.
          START_BLOCK=$(cast block-number --rpc-url http://localhost:8545)

          NUM_TRIGGERS=3
          SUCCESSFUL=0

          for i in $(seq 1 $NUM_TRIGGERS); do
            echo "=== Triggering task $i of $NUM_TRIGGERS ==="
            # Use pre-built binary for speed
            if ./target/release/send_request; then
              echo "✅ Task $i completed successfully"
              SUCCESSFUL=$((SUCCESSFUL + 1))
            else
              echo "❌ Task $i failed"
              echo "=== Router logs ==="
              kubectl logs -l app.kubernetes.io/component=router --tail 100
              exit 1
            fi

            # Small delay between triggers
            if [ $i -lt $NUM_TRIGGERS ]; then
              echo "Waiting before next trigger..."
              sleep 2
            fi
          done

          # Verify stateTransitionCount increased
          FINAL_COUNT=$(cast call "$ARRAY_SUMMATION_ADDRESS" "stateTransitionCount()(uint256)" --rpc-url http://localhost:8545 | tr -d '[:space:]')
          echo "Final stateTransitionCount: $FINAL_COUNT"
          EXPECTED=$((INITIAL_COUNT + NUM_TRIGGERS))

          if [ "$FINAL_COUNT" -ge "$EXPECTED" ]; then
            echo "✅ stateTransitionCount increased correctly: $INITIAL_COUNT -> $FINAL_COUNT"
          else
            echo "❌ stateTransitionCount mismatch: expected at least $EXPECTED, got $FINAL_COUNT"
            exit 1
          fi

          # Count SumCalculated events emitted since the triggers began.
          EVENT_TOPIC=$(cast keccak 'SumCalculated(uint256,uint256)')
          EVENT_JSON=$(cast logs --from-block "$START_BLOCK" --address "$ARRAY_SUMMATION_ADDRESS" "$EVENT_TOPIC" --rpc-url http://localhost:8545 --json 2>/dev/null || echo '[]')
          EVENT_COUNT=$(echo "$EVENT_JSON" | jq 'length')
          echo "Found $EVENT_COUNT SumCalculated events"

          if [ "$EVENT_COUNT" -ge "$NUM_TRIGGERS" ]; then
            echo "✅ Expected number of events found: $EVENT_COUNT >= $NUM_TRIGGERS"
          else
            echo "⚠️ Fewer events than expected: $EVENT_COUNT < $NUM_TRIGGERS"
          fi

          echo "✅ All $SUCCESSFUL triggers completed successfully!"

      - name: Trigger Yield Distribution via router
        # The yield distributor is a BLS-interface GasKillerSDK target; the schnorr
        # router only drives SchnorrGasKillerSDK targets, so this l2 subsystem runs
        # in the bls leg only (it is disabled at helm-install time for schnorr).
        #
        # This step mirrors what an integrator does: poll the task to `ready`, fetch the
        # rendered payload, submit it with a funded key, then assert the on-chain effect.
        if: matrix.signature_scheme == 'bls'
        run: |
          echo "=== Triggering Yield Distribution through router ==="

          YIELD_DISTRIBUTOR=$(helm get values gas-killer --all -o json | jq -r '.yieldDistribution.contractAddress')
          if [ -z "$YIELD_DISTRIBUTOR" ] || [ "$YIELD_DISTRIBUTOR" = "null" ]; then
            echo "ERROR: Could not read yieldDistribution.contractAddress from Helm values"
            helm get values gas-killer --all
            exit 1
          fi
          L2_RPC="http://localhost:8546"
          ROUTER_URL="http://localhost:8080"
          # Anvil dev account #0 (funded on the l1 and l2 forks): the account that signs and
          # submits the router-rendered payload.
          SUBMIT_KEY="0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"

          ROUTER_POD=$(kubectl get pods -l app.kubernetes.io/component=router -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
          echo "Router pod: $ROUTER_POD"
          echo "=== Router logs before trigger (last 50 lines) ==="
          kubectl logs $ROUTER_POD --tail=50 || true

          # Get current state
          INITIAL_COUNT=$(cast call --rpc-url "$L2_RPC" "$YIELD_DISTRIBUTOR" "stateTransitionCount()(uint256)" 2>/dev/null | grep -oE '^[0-9]+' | head -1 || echo "0")
          BLOCK=$(cast block-number --rpc-url "$L2_RPC" | tr -d '[:space:]')
          echo "Initial stateTransitionCount: $INITIAL_COUNT"
          echo "L2 block: $BLOCK"

          # Verify distribution is ready
          RESOLVER=$(cast call --rpc-url "$L2_RPC" "$YIELD_DISTRIBUTOR" "resolveYieldDistribution()(bool,bytes)" 2>&1)
          echo "resolveYieldDistribution: $RESOLVER"

          # Build distributeYieldGK() calldata, converted to a JSON byte array for the task request.
          CALL_DATA=$(cast calldata "distributeYieldGK()")
          echo "Calldata: $CALL_DATA"
          CALL_DATA_HEX="${CALL_DATA#0x}"
          BYTES_ARRAY="["
          for ((i=0; i<${#CALL_DATA_HEX}; i+=2)); do
            [ $i -gt 0 ] && BYTES_ARRAY+=","
            BYTES_ARRAY+="$((16#${CALL_DATA_HEX:$i:2}))"
          done
          BYTES_ARRAY+="]"

          # Submit the task to the router
          PAYLOAD="{\"body\":{\"target_address\":\"$YIELD_DISTRIBUTOR\",\"call_data\":$BYTES_ARRAY,\"transition_index\":$INITIAL_COUNT,\"from_address\":\"0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266\",\"value\":\"0\",\"block_height\":$BLOCK}}"
          echo "Payload: $PAYLOAD"

          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST -H "Content-Type: application/json" -H "Authorization: Bearer $GAS_KILLER_API_KEY" -d "$PAYLOAD" "$ROUTER_URL/trigger")
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -1)
          echo "Router response ($HTTP_CODE): $BODY"

          if [ "$HTTP_CODE" != "202" ] && [ "$HTTP_CODE" != "200" ]; then
            echo "Router rejected the trigger request"
            echo "=== Router logs ==="
            kubectl logs -l app.kubernetes.io/component=router --tail 50
            exit 1
          fi

          TASK_ID=$(echo "$BODY" | jq -r '.task_id // empty' 2>/dev/null || echo "")
          if [ -z "$TASK_ID" ]; then
            echo "Trigger response carried no task_id"
            exit 1
          fi
          echo "Task id: $TASK_ID"

          # Poll the task until the router renders its payload (or it fails), up to 300s.
          echo "Waiting for BLS aggregation to render the payload..."
          READY_TASK=""
          DUMPED_UNKNOWN=0
          for i in $(seq 1 60); do
            sleep 5
            TASK=$(curl -s -H "Authorization: Bearer $GAS_KILLER_API_KEY" "$ROUTER_URL/tasks/$TASK_ID")
            STATUS=$(echo "$TASK" | jq -r '.status // "unknown"' 2>/dev/null || echo "unknown")
            echo "  [$((i * 5))s] task status: $STATUS"
            # Surface the raw response once if it isn't a recognizable task view (e.g. an error
            # envelope), so an unexpected status query result is diagnosable from the log.
            if [ "$STATUS" = "unknown" ] && [ "$DUMPED_UNKNOWN" = "0" ]; then
              echo "  raw status response: $TASK"
              DUMPED_UNKNOWN=1
            fi
            case "$STATUS" in
              ready)
                READY_TASK="$TASK"
                break
                ;;
              failed|expired)
                echo "Task settled as '$STATUS': $(echo "$TASK" | jq -r '.error // "<none>"' 2>/dev/null)"
                kubectl logs $ROUTER_POD --tail=100 2>/dev/null || true
                exit 1
                ;;
            esac
          done

          if [ -z "$READY_TASK" ]; then
            echo "=== YIELD DISTRIBUTION TRIGGER FAILED ==="
            echo "Task $TASK_ID did not reach 'ready' within 300s"
            kubectl logs $ROUTER_POD --tail=100 2>/dev/null || true
            kubectl logs -l gas-killer/node-index=1 -c node --tail 50 || true
            exit 1
          fi

          # Submit the rendered payload ourselves (to/data are server-controlled).
          TO=$(echo "$READY_TASK" | jq -r '.payload.to' 2>/dev/null || echo "")
          DATA=$(echo "$READY_TASK" | jq -r '.payload.data' 2>/dev/null || echo "")
          echo "Submitting rendered verifyAndUpdate to $TO on L2..."
          if ! cast send "$TO" "$DATA" --private-key "$SUBMIT_KEY" --rpc-url "$L2_RPC"; then
            echo "=== YIELD DISTRIBUTION TRIGGER FAILED ==="
            echo "verifyAndUpdate submission failed"
            kubectl logs $ROUTER_POD --tail=100 2>/dev/null || true
            exit 1
          fi

          # Verify stateTransitionCount increased
          FINAL_COUNT=$(cast call --rpc-url "$L2_RPC" "$YIELD_DISTRIBUTOR" "stateTransitionCount()(uint256)" 2>/dev/null | grep -oE '^[0-9]+' | head -1 || echo "0")
          echo "Final stateTransitionCount: $FINAL_COUNT"

          if [ "$FINAL_COUNT" -gt "$INITIAL_COUNT" ] 2>/dev/null; then
            echo "=== YIELD DISTRIBUTION TRIGGER PASSED ==="
            echo "stateTransitionCount increased from $INITIAL_COUNT to $FINAL_COUNT"
          else
            echo "=== YIELD DISTRIBUTION TRIGGER FAILED ==="
            echo "stateTransitionCount did not increase (initial: $INITIAL_COUNT, final: $FINAL_COUNT)"
            echo ""
            echo "=== Router logs (last 100) ==="
            kubectl logs $ROUTER_POD --tail=100 2>/dev/null || true
            echo ""
            echo "=== Node-1 logs (last 50) ==="
            kubectl logs -l gas-killer/node-index=1 -c node --tail 50 || true
            echo ""
            echo "=== Node-2 logs (last 50) ==="
            kubectl logs -l gas-killer/node-index=2 -c node --tail 50 || true
            echo ""
            echo "=== Node-3 logs (last 50) ==="
            kubectl logs -l gas-killer/node-index=3 -c node --tail 50 || true
            exit 1
          fi

      - name: Show router logs
        run: |
          echo "=== Router logs ==="
          kubectl logs -l app.kubernetes.io/component=router --tail 50

      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== All Pods Status ==="
          kubectl get pods -o wide

          echo "=== Events (last 30) ==="
          kubectl get events --sort-by=.metadata.creationTimestamp | tail -30

          echo "=== Bridge Job Logs ==="
          kubectl logs -l app.kubernetes.io/component=bridge --tail 200 || true
          echo ""
          echo "=== Bridge Job Logs (previous container, if restarted) ==="
          BRIDGE_POD=$(kubectl get pods -l app.kubernetes.io/component=bridge -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
          if [ -n "$BRIDGE_POD" ]; then
            kubectl logs "$BRIDGE_POD" --previous --tail=200 2>/dev/null || echo "No previous bridge logs available"
          fi

          echo "=== Yield Distribution Job Logs ==="
          kubectl logs -l app.kubernetes.io/component=yield-distribution --tail 100 || true

          echo "=== Router Logs (current, last 200) ==="
          ROUTER_POD=$(kubectl get pods -l app.kubernetes.io/component=router -o jsonpath='{.items[0].metadata.name}' 2>/dev/null)
          if [ -n "$ROUTER_POD" ]; then
            echo "Restart count: $(kubectl get pod $ROUTER_POD -o jsonpath='{.status.containerStatuses[0].restartCount}' 2>/dev/null || echo unknown)"
            kubectl logs $ROUTER_POD --tail=200 || true
            echo ""
            echo "=== Router Logs (previous container, if crashed/restarted) ==="
            kubectl logs $ROUTER_POD --previous --tail=200 2>/dev/null || echo "No previous container logs available"
          fi

          echo "=== Node-1 Logs (last 100) ==="
          kubectl logs -l gas-killer/node-index=1 -c node --tail 100 || true

          echo "=== Node-2 Logs (last 100) ==="
          kubectl logs -l gas-killer/node-index=2 -c node --tail 100 || true

          echo "=== Node-3 Logs (last 100) ==="
          kubectl logs -l gas-killer/node-index=3 -c node --tail 100 || true

      - name: Cleanup
        if: always()
        run: |
          # Kill port forwarding
          pkill -f "port-forward" || true

          # Uninstall helm release
          helm uninstall gas-killer || true

          # Delete the kind cluster
          kind delete cluster --name gas-killer-test || true
