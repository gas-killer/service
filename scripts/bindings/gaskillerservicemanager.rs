use alloy::sol;

// GasKillerServiceManager bindings generated at compile time from the Foundry
// artifact (contracts/out/GasKillerServiceManager.sol/GasKillerServiceManager.json).
// Includes deploy bytecode.
sol! {
    #[sol(rpc, ignore_unlinked)]
    GasKillerServiceManager,
    "bindings/abis/GasKillerServiceManager.json"
}
