# GENERATED by scripts/gen_operator_fleet.py 10 — do not edit by hand.
services:
  eigenlayer:
    environment:
      - RPC_URL=http://ethereum:8545
      # environment wins over env_file (.env carries the historical
      # TEST_ACCOUNTS=3); the fleet size must drive registration.
      - TEST_ACCOUNTS=10
    volumes:
      - ./config/.nodes:/root/.nodes
      - ./config/config.fleet.json:/bls-middleware/contracts/docker/eigenlayer/config.json

  node-4:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=3
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc4.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3004",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4004:3004"
      - "9004:8081"
    restart: unless-stopped
  node-5:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=4
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc5.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3005",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4005:3005"
      - "9005:8081"
    restart: unless-stopped
  node-6:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=5
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc6.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3006",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4006:3006"
      - "9006:8081"
    restart: unless-stopped
  node-7:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=6
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc7.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3007",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4007:3007"
      - "9007:8081"
    restart: unless-stopped
  node-8:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=7
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc8.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3008",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4008:3008"
      - "9008:8081"
    restart: unless-stopped
  node-9:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=8
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc9.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3009",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4009:3009"
      - "9009:8081"
    restart: unless-stopped
  node-10:
    build:
      context: .
      dockerfile: node/Dockerfile
    platform: linux/amd64
    depends_on:
      eigenlayer:
        condition: service_completed_successfully
      ethereum:
        condition: service_started
    env_file:
      - .env
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_URL=${GK_SHARD_URL:-}
      - GK_SHARD_OPERATOR_ID=9
      - GK_SHARD_CONSUMER=${GK_SHARD_CONSUMER:-}
      - GK_SHARD_POLL_MS=${GK_SHARD_POLL_MS:-300}
    volumes:
      - ./config/.nodes/operator_keys/testacc10.private.bls.key.json:/app/key.json:ro
      - ./config/.nodes:/app/.nodes:ro
      - ./config/public_orchestrator.dev.json:/app/public_orchestrator.json:ro
    command:
      [
        "--key-file",
        "/app/key.json",
        "--port",
        "3010",
        "--orchestrator",
        "/app/public_orchestrator.json",
      ]
    ports:
      - "4010:3010"
      - "9010:8081"
    restart: unless-stopped

  router:
    depends_on:
      node-4:
        condition: service_started
      node-5:
        condition: service_started
      node-6:
        condition: service_started
      node-7:
        condition: service_started
      node-8:
        condition: service_started
      node-9:
        condition: service_started
      node-10:
        condition: service_started
    environment:
      - HTTP_RPC=http://ethereum:8545
      - WS_RPC=ws://ethereum:8545
      - AVS_DEPLOYMENT_PATH=/app/.nodes/avs_deploy.json
      - GK_SIM_PROFILE=${GK_SIM_PROFILE:-chain}
      - GK_SHARD_K=${GK_SHARD_K:-2}
      - GK_SHARD_OPERATORS=${GK_SHARD_OPERATORS:-10}
      - DATA_DIR=/app/data
      - ADMIN_KEY=${ADMIN_KEY:-ci-admin-key}
