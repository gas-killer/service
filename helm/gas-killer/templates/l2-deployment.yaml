{{- if and .Values.l2.enabled (eq .Values.global.environment "LOCAL") }}
{{- /* L2 (Anvil fork) deployment - only in LOCAL mode */ -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gas-killer.l2.fullname" . }}
  labels:
    {{- include "gas-killer.labels" . | nindent 4 }}
    app.kubernetes.io/component: l2
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "gas-killer.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: l2
  template:
    metadata:
      labels:
        {{- include "gas-killer.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: l2
    spec:
      containers:
        - name: l2
          image: "{{ .Values.l2.image.repository }}:{{ .Values.l2.image.tag }}"
          imagePullPolicy: {{ .Values.l2.image.pullPolicy }}
          env:
            - name: FORK_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "gas-killer.secret.fullname" . }}
                  key: L2_FORK_URL
          ports:
            - name: rpc
              containerPort: 8545
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: rpc
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 5
          readinessProbe:
            tcpSocket:
              port: rpc
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml .Values.l2.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gas-killer.l2.fullname" . }}
  labels:
    {{- include "gas-killer.labels" . | nindent 4 }}
    app.kubernetes.io/component: l2
spec:
  type: {{ .Values.l2.service.type }}
  ports:
    - port: {{ .Values.l2.service.port }}
      targetPort: rpc
      protocol: TCP
      name: rpc
  selector:
    {{- include "gas-killer.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: l2
{{- end }}
