{{- $root := . -}}
{{- $nodeCount := int .Values.global.nodeCount -}}
{{- range $i := until $nodeCount }}
{{- $nodeIndex := add $i 1 }}
{{- $nodePort := add (int $root.Values.node.basePort) $i }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gas-killer.node.fullname" $root }}-{{ $nodeIndex }}
  labels:
    {{- include "gas-killer.labels" $root | nindent 4 }}
    app.kubernetes.io/component: node
    gas-killer/node-index: {{ $nodeIndex | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "gas-killer.selectorLabels" $root | nindent 6 }}
      app.kubernetes.io/component: node
      gas-killer/node-index: {{ $nodeIndex | quote }}
  template:
    metadata:
      labels:
        {{- include "gas-killer.selectorLabels" $root | nindent 8 }}
        app.kubernetes.io/component: node
        gas-killer/node-index: {{ $nodeIndex | quote }}
    spec:
      initContainers:
        # Wait for setup job to fully complete (check for .setup_complete marker)
        # This ensures operators are registered in the quorum before nodes start
        - name: wait-for-setup
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for setup to complete..."
              KEY_FILE="/app/.nodes/operator_keys/testacc{{ $nodeIndex }}.private.bls.key.json"
              until [ -f /app/.nodes/.setup_complete ] && [ -f "$KEY_FILE" ]; do
                echo "Setup not complete, waiting for .setup_complete marker and $KEY_FILE..."
                sleep 10
              done
              echo "Setup complete! Key file found at $KEY_FILE"
          volumeMounts:
            - name: shared-data
              mountPath: /app/.nodes
              readOnly: true

        # Wait for ethereum RPC to be accessible before starting the node
        - name: wait-for-ethereum
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Waiting for Ethereum RPC..."
              until nc -z {{ include "gas-killer.ethereum.fullname" $root }} {{ $root.Values.ethereum.service.port }}; do
                echo "Ethereum not ready, waiting..."
                sleep 5
              done
              echo "Ethereum is ready!"

      containers:
        - name: node
          image: "{{ $root.Values.node.image.repository }}:{{ $root.Values.node.image.tag }}"
          imagePullPolicy: {{ $root.Values.node.image.pullPolicy }}
          args:
            - "--key-file"
            - "/app/.nodes/operator_keys/testacc{{ $nodeIndex }}.private.bls.key.json"
            - "--port"
            - {{ $nodePort | quote }}
            - "--orchestrator"
            {{- if $root.Values.orchestrator.publicConfig }}
            # Use public orchestrator from configmap if provided
            - "/app/public_orchestrator.json"
            {{- else }}
            # Use public orchestrator from the image if not provided via configmap
            - "/app/config/public_orchestrator.json"
            {{- end }}
          env:
            - name: HTTP_RPC
              value: "http://{{ include "gas-killer.ethereum.fullname" $root }}:{{ $root.Values.ethereum.service.port }}"
            - name: WS_RPC
              value: "ws://{{ include "gas-killer.ethereum.fullname" $root }}:{{ $root.Values.ethereum.service.port }}"
            - name: AVS_DEPLOYMENT_PATH
              value: "/app/.nodes/avs_deploy.json"
          ports:
            - name: node
              containerPort: {{ $nodePort }}
              protocol: TCP
          # Use exec probe to check if process is running
          # Check /proc/1/cmdline since our process is PID 1 in the container
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - "cat /proc/1/cmdline | grep -q gas-killer"
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 60
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - "cat /proc/1/cmdline | grep -q gas-killer"
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 5
          volumeMounts:
            - name: shared-data
              mountPath: /app/.nodes
              readOnly: true
            {{- if $root.Values.orchestrator.publicConfig }}
            - name: config
              mountPath: /app/public_orchestrator.json
              subPath: public_orchestrator.json
              readOnly: true
            {{- end }}
          resources:
            {{- toYaml $root.Values.node.resources | nindent 12 }}

      volumes:
        - name: shared-data
          persistentVolumeClaim:
            claimName: {{ include "gas-killer.shareddata.fullname" $root }}
        {{- if $root.Values.orchestrator.publicConfig }}
        - name: config
          configMap:
            name: {{ include "gas-killer.config.fullname" $root }}
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gas-killer.node.fullname" $root }}-{{ $nodeIndex }}
  labels:
    {{- include "gas-killer.labels" $root | nindent 4 }}
    app.kubernetes.io/component: node
    gas-killer/node-index: {{ $nodeIndex | quote }}
spec:
  type: ClusterIP
  # CRITICAL: Allow traffic to pods before they pass readiness probes
  # This is required because nodes need to dial each other during P2P startup
  # Without this, services have no endpoints until pods are ready, creating a deadlock
  publishNotReadyAddresses: true
  ports:
    - port: {{ $nodePort }}
      targetPort: node
      protocol: TCP
      name: node
  selector:
    {{- include "gas-killer.selectorLabels" $root | nindent 4 }}
    app.kubernetes.io/component: node
    gas-killer/node-index: {{ $nodeIndex | quote }}
{{- end }}
